#include <bits/stdc++.h>

using namespace std;

const int MAXN = 2e5 + 1000;

pair<int, int> guards[MAXN];

#define left first
#define right second

int main() {
    int N, Q;
    scanf(" %d %d", &N, &Q);

    for (int i=0; i<N; i++) {
        scanf(" %d %d", guards[i].left, guards[i].right);
    }

    char op;
    int id, l, r;
    for (int i=0; i<Q; i++) {

    }

    return 0;
}
